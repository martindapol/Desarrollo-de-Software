function procesar(){
    let $cant = document.getElementById('cant') //caja cantidad
    let n = parseInt($cant.value) // número de vueltas del ciclo for

    for (let i = 0; i < n; i++) {
       let temp = parseInt(Math.random()*50) // devuelve un número entre [0;1)
       console.log(temp) 

        //Desarrollar la lógica necesaria para resolver los 2 último puntos :

    } 

}